name: planit
description:  PLANIT is an e-commerce platform designed for seamless management 
              of products by business administrators, while customers can 
              conveniently make purchases through Android and iOS applications. 
              You can access the platform via the following web link- 
              https://planit.club/. The project has been developed using the 
              Flutter/Bloc Framework.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.2.1
  auto_route: ^7.9.0
  flutter_bloc: ^8.1.4
  json_annotation: ^4.8.1
  freezed_annotation: ^2.4.1
  get_it: ^7.6.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.8
  json_serializable: ^6.7.1
  freezed: ^2.4.7
  auto_route_generator: ^7.3.2
  flutter_native_splash : ^2.4.0

flutter:
  uses-material-design: true

assets:
    - assets/images/

flutter_icons:
  image_path: assets/images/launcher_icon.png
  android: true
  adaptive_icon_background: "#023507"
  adaptive_icon_foreground: assets/images/launcher_icon.png
  ios: true
  remove_alpha_ios: true
  web:
    generate: true

flutter_native_splash:
  android: true
  ios: true
  web: true
  color: "#FFFFFF"
  image: assets/images/launcher_icon.png
  ios_content_mode: center
  web_image_mode: center
  fullscreen: true